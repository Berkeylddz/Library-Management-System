/**
 The {@code Academic} class represents an academic member in a library system.
 It extends the {@link Member} class and inherits its properties and methods.
 @author [Author Name]
 @version [Date]
 */
public class Academic extends Member {
    public Academic(int id) {
        super(id);
    }
}
